<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence
                                 https://jakarta.ee/xml/ns/persistence/persistence_3_1.xsd"
             version="3.1">

    <persistence-unit name="bankingPU" transaction-type="JTA">
        <jta-data-source>java:jboss/datasources/BankingDS</jta-data-source>

        <class>org.ejblab.banking.domain.Customer</class>
        <class>org.ejblab.banking.domain.Account</class>
        <class>org.ejblab.banking.domain.Transfer</class>
        <class>org.ejblab.banking.domain.LedgerEntry</class>

        <exclude-unlisted-classes>true</exclude-unlisted-classes>

        <properties>
            <!-- We use Flyway for schema migrations; Hibernate only validates. -->
            <property name="jakarta.persistence.schema-generation.database.action" value="none"/>
            <property name="hibernate.hbm2ddl.auto" value="validate"/>
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>
            <property name="hibernate.show_sql" value="false"/>
            <property name="hibernate.format_sql" value="false"/>
            <!-- Batch support used by the benchmark in LoadDataBean -->
            <property name="hibernate.jdbc.batch_size" value="50"/>
            <property name="hibernate.order_inserts" value="true"/>
            <property name="hibernate.order_updates" value="true"/>
        </properties>
    </persistence-unit>
</persistence>
